### suppress all include paths but the current directory

registry "{_MI_REG_INCLUDE}" 
	value "."  
end registry

### suppress all library search paths but the current directory
### Path to gen_llvm plugin need to be specified with the
### -L command line.
registry "{_MI_REG_LIBRARY}" 
	value "."
end registry

### enable MetaSL LLVM backend
registry "{_MI_REG_METASL_BACKEND}"
	value "LLVM"
end registry

